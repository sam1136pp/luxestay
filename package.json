{
  "name": "hotel-booking-root",
  "version": "1.0.0",
  "description": "Root package.json for Railway Deployment",
  "scripts": {
    "install": "cd server && npm install",
    "start": "cd server && npm start"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
